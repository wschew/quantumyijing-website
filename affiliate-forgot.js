const $=s=>document.querySelector(s);$('#send').addEventListener('click',async()=>{const s=$('#status');s.textContent='Sending…';await fetch('/api/affiliate/auth/forgot',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:$('#email').value})});s.className='status success';s.textContent='If the account is eligible, a reset email has been sent. / 如账户符合资格，重设电邮已发送。';});
