# Cloudflare Pages encrypted secrets / bindings (do not commit real secret values)
RESEND_API_KEY=
ADMIN_TOKEN=
# ENQUIRIES_DB is a Cloudflare D1 binding, not a text environment variable.

# v2.7 commerce foundation — not active until checkout integration is implemented
SENANGPAY_MERCHANT_ID=
SENANGPAY_SECRET_KEY=
# Never place SENANGPAY_SECRET_KEY in client-side code or GitHub. Store it as a Cloudflare encrypted secret.
