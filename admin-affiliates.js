const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const token=()=>$('#token').value.trim();async function load(){if(!token()){status.textContent='Enter Admin Token';return}const f=$('#filter').value,r=await fetch('/api/admin/affiliates'+(f?`?status=${f}`:''),{headers:{authorization:`Bearer ${token()}`}}),d=await r.json();if(!r.ok){$('#status').textContent=d.error||r.status;return}$('#status').textContent=`${d.results.length} record(s)`;$('#rows').innerHTML=d.results.map(a=>`<tr><td>${a.id}</td><td>${esc(a.affiliate_code)}</td><td>${esc(a.full_name)}</td><td>${esc(a.email)}</td><td>${esc(a.phone)}</td><td>${esc(a.country)}</td><td>${esc(a.account_type)}</td><td>${esc(a.bank_name)}<div class="muted">${esc(a.bank_account_name)} • ${esc(a.bank_account_number)}</div></td><td>${esc(a.status)}</td><td>${esc(a.membership_expires_at||'')}</td><td class="actions">${a.status==='Pending'?`<button class="approve" data-id="${a.id}" data-action="approve">Approve</button><button class="reject" data-id="${a.id}" data-action="reject">Reject</button>`:''}</td></tr>`).join('')}async function act(id,action){const r=await fetch('/api/admin/affiliates',{method:'POST',headers:{authorization:`Bearer ${token()}`,'content-type':'application/json'},body:JSON.stringify({id:+id,action})}),d=await r.json();if(!r.ok){alert(d.error||r.status);return}if(d.affiliate_code)alert(`Approved: ${d.affiliate_code}`);load()}$('#load').onclick=load;$('#rows').onclick=e=>{const b=e.target.closest('button[data-action]');if(b)act(b.dataset.id,b.dataset.action)}