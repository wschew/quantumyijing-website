# v3.0.1 install

1. Replace `functions/product/[slug].js` with the included file.
2. Append the contents of `product-funnel-v3.0.1.css` to the bottom of your existing `product-funnel.css`.
3. Run `database/migrate-v3.0.1.sql` in Cloudflare D1 ONCE only.
4. Commit: `Add v3.0.1 long-form bilingual course descriptions`
5. Push origin and test the YJ12 page in Cloudflare Preview.
