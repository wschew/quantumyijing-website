# Documentation only. Never place real secrets in this file or commit them to GitHub.
RESEND_API_KEY=replace_with_resend_key
ADMIN_TOKEN=replace_with_long_random_admin_token

# ENQUIRIES_DB is a Cloudflare D1 binding configured in the Pages dashboard,
# not a text environment variable.
