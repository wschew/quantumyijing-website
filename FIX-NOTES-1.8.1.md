# Version 1.8.1 Fix Notes

- Corrected the missing closing `>` in the `academic-programmes.css` stylesheet link.
- Restored loading of `research.css`.
- Updated cache-busting asset references to `v=1.8.1`.
- Retained both publication cover images in the `images` folder.
