const $=s=>document.querySelector(s),money=v=>`MYR ${Number(v||0).toFixed(2)}`,token=()=>localStorage.getItem('qy_coach_token')||'';async function load(){const r=await fetch('/api/coach/me',{headers:{authorization:`Bearer ${token()}`},cache:'no-store'}),d=await r.json().catch(()=>({}));if(!r.ok){localStorage.removeItem('qy_coach_token');location.href='/coach-login';return}$('#welcome').textContent=`Welcome, ${d.coach.display_name||d.coach.full_name}`;$('#code').textContent=`Coach Code: ${d.coach.coach_code}`;const a=d.assignments||[];$('#mCourses').textContent=a.length;$('#mParticipants').textContent=a.reduce((s,x)=>s+Number(x.participant_count||0),0);$('#mPotential').textContent=money(a.reduce((s,x)=>s+Number(x.potential_commission||0),0));$('#rows').innerHTML=a.length?a.map(x=>`<tr><td>${x.course_sku||''}<br><b>${x.course_name||''}</b></td><td>${x.course_start_date||'—'}<br>${x.course_end_date||''}</td><td>${x.course_status}</td><td>${x.participant_count}${Number(x.participant_count_locked)?' 🔒':''}</td><td>${x.commission_mode==='fixed_per_participant'?money(x.commission_fixed_amount)+' / participant':x.commission_mode==='percentage_of_revenue'?Number(x.commission_rate||0).toFixed(2)+'% of revenue':money(x.commission_fixed_amount)+' / course'}</td><td><b>${money(x.potential_commission)}</b></td><td>${x.final_commission==null?'—':money(x.final_commission)}</td><td>${x.payout_eligible_month||'—'}</td></tr>`).join(''):'<tr><td colspan="8">No assigned courses yet.</td></tr>'}$('#logout').onclick=async()=>{try{await fetch('/api/coach/logout',{method:'POST',headers:{authorization:`Bearer ${token()}`}})}catch{}localStorage.removeItem('qy_coach_token');location.href='/coach-login'};load();
