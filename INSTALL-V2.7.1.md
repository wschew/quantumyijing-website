# Install v2.7.1

1. Confirm GitHub Desktop is on `v2-development`.
2. Extract the v2.7.1 Update package over the existing repository and replace matching files.
3. No D1 migration is required.
4. Commit with: `Fix v2.7 bilingual marketing framework`.
5. Push origin and wait for the Cloudflare Preview deployment.
6. Test `/lp/membership.html`, `/events.html`, `/promotions.html`, `/news.html`, `/products.html`, and the legal pages in both EN and 中文.
7. Do not merge to `main` until Preview testing passes.
