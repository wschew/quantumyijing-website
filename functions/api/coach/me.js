import {requireCoach} from './_auth.js';
function potential(x){if(x.commission_mode==='fixed_per_participant')return Number(x.participant_count||0)*Number(x.commission_fixed_amount||0);if(x.commission_mode==='percentage_of_revenue')return Number(x.eligible_course_revenue||0)*Number(x.commission_rate||0)/100;return Number(x.commission_fixed_amount||0)}
export async function onRequestGet({request,env}){const a=await requireCoach(request,env);if(a.error)return a.error;const rows=await a.db.prepare(`SELECT * FROM coach_course_assignments WHERE coach_id=? ORDER BY course_start_date DESC,id DESC`).bind(a.coach.id).all();const assignments=(rows.results||[]).map(x=>({...x,potential_commission:potential(x)}));return Response.json({coach:{coach_code:a.coach.coach_code,full_name:a.coach.full_name,display_name:a.coach.display_name,email:a.coach.email,status:a.coach.status},assignments},{headers:{'cache-control':'no-store'}})}
